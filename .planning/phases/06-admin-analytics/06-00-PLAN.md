---
phase: 06-admin-analytics
plan: 00
type: execute
wave: 0
depends_on: []
files_modified: [tests/unit/admin/revenue.test.ts, tests/unit/admin/funnel.test.ts, tests/unit/admin/costs.test.ts, tests/unit/admin/user-management.test.ts]
autonomous: true
requirements: [ADMIN-01, ADMIN-02, ADMIN-03, ADMIN-04]
user_setup: []

must_haves:
  truths:
    - "tests/unit/admin/ directory exists"
    - "Stub test files exist for all 4 requirements (revenue, funnel, costs, user-management)"
    - "Vitest can discover and run admin tests"
  artifacts:
    - path: "tests/unit/admin/revenue.test.ts"
      provides: "Stub tests for revenue dashboard (ADMIN-01, ADMIN-02)"
      min_lines: 20
    - path: "tests/unit/admin/funnel.test.ts"
      provides: "Stub tests for conversion funnel (ADMIN-01)"
      min_lines: 15
    - path: "tests/unit/admin/costs.test.ts"
      provides: "Stub tests for cost analysis (ADMIN-03)"
      min_lines: 15
    - path: "tests/unit/admin/user-management.test.ts"
      provides: "Stub tests for user management (ADMIN-04)"
      min_lines: 20
  key_links: []
---

<objective>
Create test infrastructure for Phase 6 admin analytics features.

Purpose: Establish test directory structure and stub files so Plans 06-01 and 06-02 can reference them in verification commands.
Output: Empty test directory with stub test files containing describe blocks and placeholder tests
</objective>

<execution_context>
@/home/cativo23/.claude/get-shit-done/workflows/execute-plan.md
@/home/cativo23/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-admin-analytics/06-VALIDATION.md
@vitest.config.ts
@tests/setup.ts
@tests/unit/admin-middleware.test.ts
</context>

<interfaces>
<!-- Existing test patterns from the codebase -->

From tests/unit/admin-middleware.test.ts:
```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest'

describe('Admin Middleware', () => {
  it('should require admin authentication', () => {
    // Test implementation
  })
})
```

From tests/unit/server/utils/auth.test.ts:
```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest'

describe('Auth Utility', () => {
  describe('requireAdmin', () => {
    it('should throw if user is not admin', () => {
      // Test implementation
    })
  })
})
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create admin test directory and stub test files</name>
  <files>tests/unit/admin/revenue.test.ts, tests/unit/admin/funnel.test.ts, tests/unit/admin/costs.test.ts, tests/unit/admin/user-management.test.ts</files>
  <action>
    1. Create directory `tests/unit/admin/`

    2. Create `tests/unit/admin/revenue.test.ts` with:
       - Import vitest: `import { describe, it, expect, vi, beforeEach } from 'vitest'`
       - Describe block: `describe('Revenue API', () => { ... })`
       - Stub tests (with empty it blocks or skip):
         - `it('returns revenue data with time range aggregation', () => { ... })`
         - `it('returns gross and net revenue in summary', () => { ... })`
         - `it('supports day/week/month/quarter range params', () => { ... })`
       - Add comment: `// TODO: Implement revenue API tests in Plan 06-01`

    3. Create `tests/unit/admin/funnel.test.ts` with:
       - Import vitest
       - Describe block: `describe('Funnel API', () => { ... })`
       - Stub tests:
         - `it('returns 4-stage conversion funnel', () => { ... })`
         - `it('calculates conversion rates between stages', () => { ... })`
       - Add comment: `// TODO: Implement funnel API tests in Plan 06-01`

    4. Create `tests/unit/admin/costs.test.ts` with:
       - Import vitest
       - Describe block: `describe('Cost Analysis API', () => { ... })`
       - Stub tests:
         - `it('returns cost breakdown by tier', () => { ... })`
         - `it('calculates profit margin per tier', () => { ... })`
         - `it('includes AI cost calculations from token usage', () => { ... })`
       - Add comment: `// TODO: Implement cost analysis tests in Plan 06-02`

    5. Create `tests/unit/admin/user-management.test.ts` with:
       - Import vitest
       - Describe block: `describe('User Management API', () => { ... })`
       - Stub tests:
         - `it('allows admin to add credits to user', () => { ... })`
         - `it('requires reason for credit adjustments', () => { ... })`
         - `it('allows admin to suspend user account', () => { ... })`
         - `it('requires reason for suspension', () => { ... })`
       - Add comment: `// TODO: Implement user management tests in Plan 06-02`
  </action>
  <verify>
    <automated>npm run test -- tests/unit/admin/ --run</automated>
  </verify>
  <done>
    - tests/unit/admin/ directory exists
    - All 4 test files created with describe blocks and stub tests
    - npm run test -- tests/unit/admin/ --run executes without file errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify Vitest discovers admin tests</name>
  <files>vitest.config.ts</files>
  <action>
    1. Run `npm run test -- tests/unit/admin/ --run` to verify test discovery
    2. Confirm Vitest finds all 4 test files (may show pending/skipped tests)
    3. If tests don't run, verify:
       - File pattern in vitest.config.ts includes tests/unit/admin/*.test.ts
       - Import paths are correct
       - No syntax errors in test files
  </action>
  <verify>
    <automated>npm run test -- tests/unit/admin/ --run</automated>
  </verify>
  <done>
    Vitest successfully discovers and runs tests in tests/unit/admin/ directory
  </done>
</task>

</tasks>

<verification>
<automated>
  - npm run test -- tests/unit/admin/ --run executes without "file not found" errors
  - All 4 test files exist: revenue.test.ts, funnel.test.ts, costs.test.ts, user-management.test.ts
</automated>
</verification>

<success_criteria>
- Test directory structure created for Phase 6 admin features
- Plans 06-01 and 06-02 can now reference test files in verification commands
- ADMIN-01/02/03/04 all have corresponding test stubs
</success_criteria>

<output>
After completion, create `.planning/phases/06-admin-analytics/06-00-SUMMARY.md` documenting:
- Test file locations and structure
- Stub test count per file
- Confirmation that Vitest discovers all admin tests
</output>

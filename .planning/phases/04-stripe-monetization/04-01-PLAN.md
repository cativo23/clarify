---
phase: 04-stripe-monetization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/utils/stripe-client.ts
  - server/api/stripe/checkout.post.ts
  - server/api/stripe/webhook.post.ts
  - database/migrations/20260217000001_atomic_credit_increment.sql
  - types/index.ts
  - docs/STRIPE_SETUP.md
autonomous: true
requirements: [STRIPE-01, STRIPE-02, STRIPE-03]
user_setup:
  - service: stripe
    why: "Credit-based monetization system"
    env_vars:
      - name: STRIPE_SECRET_KEY
        source: "Stripe Dashboard -> Developers -> API keys"
      - name: STRIPE_PUBLISHABLE_KEY
        source: "Stripe Dashboard -> Developers -> API keys"
      - name: STRIPE_WEBHOOK_SECRET
        source: "Stripe Dashboard -> Developers -> Webhooks -> Signing secret"
    dashboard_config:
      - task: "Create webhook endpoint"
        location: "Stripe Dashboard -> Developers -> Webhooks"
        url: "https://your-domain.com/api/stripe/webhook"
      - task: "Create credit packages as Products/Prices"
        location: "Stripe Dashboard -> Products"

must_haves:
  truths:
    - "User can select credit package and complete Stripe checkout"
    - "Payment successful triggers atomic credit increment via webhook"
    - "Transaction is logged in transactions table"
    - "Stripe Price IDs are properly configured"
  artifacts:
    - path: "server/utils/stripe-client.ts"
      provides: "Stripe client utilities and credit packages configuration"
    - path: "server/api/stripe/checkout.post.ts"
      provides: "Checkout session creation endpoint"
    - path: "server/api/stripe/webhook.post.ts"
      provides: "Webhook handler for payment completion"
    - path: "database/migrations/20260217000001_atomic_credit_increment.sql"
      provides: "Atomic credit increment function"
  key_links:
    - from: "pages/credits.vue"
      to: "server/api/stripe/checkout.post.ts"
      via: "API call to create checkout session"
      pattern: "\\$fetch\\(\"/api/stripe/checkout\""
    - from: "server/api/stripe/webhook.post.ts"
      to: "database/migrations/20260217000001_atomic_credit_increment.sql"
      via: "RPC function call"
      pattern: "increment_user_credits"
    - from: "server/utils/stripe-client.ts"
      to: "CREDIT_PACKAGES"
      via: "Configuration"
      pattern: "CREDIT_PACKAGES"
---

<objective>
Complete the Stripe integration for credit purchases with the specified packages (5/$4.99, 10/$8.99, 25/$19.99) ensuring atomic credit updates and proper transaction logging.
</objective>

<execution_context>
@/home/cativo23/.claude/get-shit-done/workflows/execute-plan.md
@/home/cativo23/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Finalize Stripe Configuration and Credit Packages</name>
  <files>server/utils/stripe-client.ts</files>
  <action>
    Complete the Stripe client implementation with proper credit package configuration and ensure the price IDs match those that will be created in Stripe dashboard. Set up the exact credit packages as specified: 5 credits for $4.99, 10 credits for $8.99, 25 credits for $19.99. Ensure the price IDs in the constants match what will be configured in the Stripe dashboard.
  </action>
  <verify>
    <automated>npm run lint server/utils/stripe-client.ts</automated>
  </verify>
  <done>Credit packages match the specified pricing (5/$4.99, 10/$8.99, 25/$19.99) with placeholder price IDs ready for configuration</done>
</task>

<task type="auto">
  <name>Task 2: Complete Webhook Handler for Atomic Credit Updates</name>
  <files>server/api/stripe/webhook.post.ts, server/utils/stripe-client.ts</files>
  <action>
    Ensure the webhook handler properly processes checkout.session.completed events and calls the atomic credit update function. Verify that the handleWebhookEvent function properly extracts user ID and credit amounts from session metadata and updates the user's credits via the increment_user_credits RPC function. Ensure transaction logging occurs after successful credit updates.
  </action>
  <verify>
    <automated>npm run lint server/api/stripe/webhook.post.ts &amp;&amp; npm run lint server/utils/stripe-client.ts</automated>
  </verify>
  <done>Webhook successfully processes payments and atomically increments user credits via RPC function</done>
</task>

<task type="auto">
  <name>Task 3: Document Stripe Setup Process</name>
  <files>docs/STRIPE_SETUP.md</files>
  <action>
    Create documentation that explains how to set up Stripe for the Clarify application, including how to create the required credit packages in the Stripe dashboard, configure webhooks, and set the appropriate environment variables. Include instructions for creating the three credit packages with their specific pricing.
  </action>
  <verify>
    <automated>cat docs/STRIPE_SETUP.md | wc -l</automated>
  </verify>
  <done>Complete setup guide exists for configuring Stripe integration including price creation and webhook configuration</done>
</task>

</tasks>

<verification>
1. Verify that the credit packages in stripe-client.ts match the requirements: 5/$4.99, 10/$8.99, 25/$19.99
2. Confirm that webhook handler properly processes checkout completion and updates credits atomically
3. Ensure transaction logging works correctly in the database
4. Test that all environment variables are properly referenced
</verification>

<success_criteria>
Users can select credit packages (5/$4.99, 10/$8.99, 25/$19.99), complete checkout via Stripe, and receive credits automatically through the webhook system with atomic updates and proper transaction logging.
</success_criteria>

<output>
After completion, create `.planning/phases/04-stripe-monetization/04-01-SUMMARY.md`
</output>
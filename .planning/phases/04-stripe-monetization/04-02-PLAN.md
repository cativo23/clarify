---
phase: 04-stripe-monetization
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - pages/credits.vue
  - nuxt.config.ts
  - server/utils/redirect-validation.ts
  - types/index.ts
autonomous: true
requirements: [STRIPE-01, STRIPE-02]
user_setup: []

must_haves:
  truths:
    - "User can see credit packages with correct pricing on credits page"
    - "Checkout flow redirects to Stripe securely"
    - "Success/cancel redirects work properly after payment"
    - "Environment variables are properly configured"
  artifacts:
    - path: "pages/credits.vue"
      provides: "User interface for selecting and purchasing credit packages"
    - path: "nuxt.config.ts"
      provides: "Stripe configuration in runtime config"
    - path: "server/utils/redirect-validation.ts"
      provides: "Secure redirect URL validation"
  key_links:
    - from: "pages/credits.vue"
      to: "server/api/stripe/checkout.post.ts"
      via: "HTTP request"
      pattern: "\\$fetch\\(\"/api/stripe/checkout\""
    - from: "nuxt.config.ts"
      to: "runtimeConfig"
      via: "configuration"
      pattern: "stripePublishableKey"
---

<objective>
Implement the frontend credit purchase experience and configure environment variables to complete the end-to-end payment flow.
</objective>

<execution_context>
@/home/cativo23/.claude/get-shit-done/workflows/execute-plan.md
@/home/cativo23/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-stripe-monetization/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify and Enhance Credit Purchase UI</name>
  <files>pages/credits.vue</files>
  <action>
    Review the existing credits page to ensure it displays the correct credit packages with accurate pricing. Ensure the UI properly integrates with the checkout endpoint and provides appropriate feedback during the purchase process. Add any missing features or improvements to enhance user experience.
  </action>
  <verify>
    <automated>npm run lint pages/credits.vue</automated>
  </verify>
  <done>Credits page displays all three packages correctly with proper pricing and purchase flow</done>
</task>

<task type="auto">
  <name>Task 2: Configure Runtime Environment Variables</name>
  <files>nuxt.config.ts</files>
  <action>
    Ensure Stripe publishable key is properly configured in the runtime configuration and made available to the client-side code. Verify that the public runtime configuration includes the Stripe publishable key for the frontend to initialize Stripe.js.
  </action>
  <verify>
    <automated>grep -n "stripe" nuxt.config.ts</automated>
  </verify>
  <done>Stripe publishable key is available to client-side code via runtime config</done>
</task>

<task type="auto">
  <name>Task 3: Verify Secure Redirect Validation</name>
  <files>server/utils/redirect-validation.ts</files>
  <action>
    Create or verify the redirect validation utility that prevents open redirect vulnerabilities. The utility should only allow redirects to approved domains to prevent phishing attacks. This is referenced in the checkout endpoint but may need to be implemented.
  </action>
  <verify>
    <automated>ls server/utils/redirect-validation.ts</automated>
  </verify>
  <done>Secure redirect validation utility exists and is used by the checkout endpoint</done>
</task>

</tasks>

<verification>
1. Check that the credits page UI properly displays the credit packages
2. Verify environment variables are correctly configured
3. Ensure the redirect validation utility is in place and secure
4. Confirm the purchase flow works from UI to Stripe checkout
</verification>

<success_criteria>
Frontend credit purchase flow is complete and secure, with proper environment configuration and validation.
</success_criteria>

<output>
After completion, create `.planning/phases/04-stripe-monetization/04-02-SUMMARY.md`
</output>
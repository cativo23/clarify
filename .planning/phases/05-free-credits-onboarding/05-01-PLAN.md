---
phase: 05-free-credits-onboarding
plan: 01
type: execute
wave: 1
depends_on: ["05-00"]
files_modified:
  - database/migrations/20260303000001_add_free_credit_fields.sql
autonomous: true
requirements:
  - CREDIT-01
  - CREDIT-02

must_haves:
  truths:
    - New users receive 10 credits automatically after email verification
    - Users can get 1 free Basic analysis per month regardless of credit balance
    - Free credits are only awarded after email verification
  artifacts:
    - path: "database/migrations/20260303000001_add_free_credit_fields.sql"
      provides: "Database schema changes for free credits and monthly free analysis tracking"
    - path: "users"
      provides: "New columns for tracking free credits and monthly analysis usage"
  key_links:
    - from: "users table"
      to: "auth.users table"
      via: "email verification trigger"
      pattern: "email_confirmed_at IS NOT NULL"
---

<objective>
Add database schema changes to support free credits on signup and monthly free Basic analysis.

Purpose: Extend the users table with fields needed to track free credits and monthly free analysis usage
Output: Migration file that adds necessary columns to users table
</objective>

<execution_context>
@/home/cativo23/.claude/get-shit-done/workflows/execute-plan.md
@/home/cativo23/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@database/migrations/20260216000000_create_core_schema.sql
@database/migrations/20260216000001_fix_credit_deduction_race_condition.sql
@database/migrations/20260217000001_atomic_credit_increment.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create migration for free credit fields</name>
  <files>database/migrations/20260303000001_add_free_credit_fields.sql</files>
  <action>Create a new migration file that adds the following columns to the users table:
  - free_credits_awarded (BOOLEAN, DEFAULT FALSE) - Tracks if signup free credits were awarded
  - free_credits_at (TIMESTAMP WITH TIME ZONE) - Timestamp when free credits were awarded
  - monthly_free_analysis_used (BOOLEAN, DEFAULT FALSE) - Tracks if user has used monthly free analysis
  - monthly_free_analysis_reset_date (DATE) - Date when monthly free analysis will reset
  - monthly_free_analysis_counter (INTEGER, DEFAULT 0) - Counter for free analyses in current period

  Also create a database trigger function that awards 10 credits to a user when their email is confirmed in auth.users, but only if they haven't already received free credits.

  The trigger should check that email_confirmed_at is changing from NULL to a value (indicating first verification) before awarding credits.</action>
  <verify>npx vitest run tests/integration/free-credits.spec.ts --reporter=verbose</verify>
  <done>New columns exist in users table schema and trigger function is defined to award credits on email verification</done>
</task>

</tasks>

<verification>
Check that the migration file contains the required ALTER TABLE statements and trigger function. Verify with: npx vitest run tests/integration/free-credits.spec.ts
</verification>

<success_criteria>
Migration file successfully defines schema changes and email verification trigger
</success_criteria>

<output>
After completion, create `.planning/phases/05-free-credits-onboarding/05-01-SUMMARY.md`
</output>
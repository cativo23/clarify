---
phase: 05-free-credits-onboarding
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - components/demo/InteractiveDemo.vue
  - server/api/demo/simulate.post.ts
  - pages/index.vue
autonomous: true
requirements:
  - DEMO-01

must_haves:
  truths:
    - Homepage has interactive demo that shows product value
    - Demo simulates analysis experience without consuming AI resources
    - Demo provides realistic preview of actual product
    - Demo resets on each visit/session
  artifacts:
    - path: "components/demo/InteractiveDemo.vue"
      provides: "Interactive demo component for homepage"
    - path: "server/api/demo/simulate.post.ts"
      provides: "Simulation endpoint for demo analysis"
    - path: "pages/index.vue"
      provides: "Integration of demo component on homepage"
  key_links:
    - from: "homepage"
      to: "interactive demo"
      via: "button click"
      pattern: "Demo button triggers InteractiveDemo component"
    - from: "InteractiveDemo component"
      to: "simulate endpoint"
      via: "API call"
      pattern: "fetch('/api/demo/simulate')"
---

<objective>
Create interactive demo on homepage to showcase product value without requiring sign-up.

Purpose: Implement simulation-based demo that mimics analysis experience without using AI resources
Output: Interactive demo component integrated into homepage with simulation API
</objective>

<execution_context>
@/home/cativo23/.claude/get-shit-done/workflows/execute-plan.md
@/home/cativo23/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@pages/index.vue
@components/Dropzone.vue
@components/analysis/AnalysisCard.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create demo simulation API endpoint</name>
  <files>server/api/demo/simulate.post.ts</files>
  <action>Create a new API endpoint that simulates an analysis without calling AI services:

1. Use Zod validation to accept a contract_name parameter (similar to the real analyze endpoint)
2. Generate simulated analysis results with:
   - Mock summary_json containing sample findings
   - Sample risk_level (randomly low/medium/high)
   - Fake risk items, key_clauses, recommendations
   - Sample values that match what real analyses produce
3. Return results in same format as real analysis endpoint
4. Add rate limiting to prevent abuse (e.g., 10 requests per hour per IP)
5. Don't require authentication (public endpoint)
6. Include appropriate headers to indicate this is a simulation</action>
  <verify>npx vitest run tests/e2e/demo-flow.spec.ts --reporter=verbose</verify>
  <done>Simulation endpoint returns realistic demo results without AI consumption</done>
</task>

<task type="auto">
  <name>Task 2: Create interactive demo component</name>
  <files>components/demo/InteractiveDemo.vue</files>
  <action>Create a Vue component that implements the interactive demo experience:

1. Use script setup with TypeScript
2. Include a simulated upload experience (fake progress bar)
3. Call the demo/simulate endpoint when user submits
4. Display simulated analysis results in the same format as real analyses
5. Use Tailwind CSS with the project's styling patterns (glassmorphism, dark mode, etc.)
6. Include sample documents that users can try (click to select)
7. Reset state when component is unmounted or when user wants to try again
8. Show loading states and simulated processing time
9. Match the visual design of the real analysis results (risk cards, collapsible sections, etc.)</action>
  <verify>npx vitest run tests/e2e/demo-flow.spec.ts --reporter=verbose</verify>
  <done>Interactive demo component functions and displays simulated results</done>
</task>

<task type="auto">
  <name>Task 3: Integrate demo component into homepage</name>
  <files>pages/index.vue</files>
  <action>Modify the homepage to integrate the interactive demo:

1. Import the InteractiveDemo component
2. Update the "Ver Demo" button to open the demo experience (either as modal or expanded section)
3. Ensure the demo integrates smoothly with the existing design
4. Update the button text to something like "Probar Demo" or "Ver Ejemplo"
5. Consider how the demo state should be managed (reset on close, etc.)
6. Maintain responsive design and accessibility
7. Ensure it works well with the existing theme toggle and dark mode</action>
  <verify>npx playwright test tests/e2e/demo-flow.spec.ts</verify>
  <done>Demo component is accessible from homepage and provides realistic preview of product</done>
</task>

</tasks>

<verification>
Homepage demo works and provides realistic preview of actual analysis experience. Verify with: npx playwright test tests/e2e/demo-flow.spec.ts
</verification>

<success_criteria>
Interactive demo is available on homepage and demonstrates product value effectively
</success_criteria>

<output>
After completion, create `.planning/phases/05-free-credits-onboarding/05-03-SUMMARY.md`
</output>
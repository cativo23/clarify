---
phase: 05-free-credits-onboarding
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - server/api/analyze.post.ts
  - database/migrations/20260303000002_add_email_verification_trigger.sql
autonomous: true
requirements:
  - CREDIT-01
  - CREDIT-02

must_haves:
  truths:
    - New users receive 10 credits automatically after email verification
    - Users get 1 free Basic analysis per month regardless of credit balance
    - Free analysis only applies to Basic tier
    - Monthly free analysis resets at the start of each calendar month
  artifacts:
    - path: "database/migrations/20260303000002_add_email_verification_trigger.sql"
      provides: "Email verification trigger that awards 10 free credits"
    - path: "server/api/analyze.post.ts"
      provides: "Logic to allow free Basic analysis if user qualifies"
  key_links:
    - from: "auth.users table"
      to: "public.users table"
      via: "trigger_award_free_credits_on_verification"
      pattern: "UPDATE OF email_confirmed_at"
    - from: "analyze endpoint"
      to: "users table"
      via: "monthly_free_analysis check"
      pattern: "monthly_free_analysis_used and analysis_type === 'basic'"
---

<objective>
Implement email verification trigger to award free credits and modify analysis endpoint to handle monthly free Basic analysis.

Purpose: Set up automatic awarding of 10 free credits when users verify their email, and enable monthly free Basic analysis
Output: Database trigger and updated analysis endpoint with free analysis logic
</objective>

<execution_context>
@/home/cativo23/.claude/get-shit-done/workflows/execute-plan.md
@/home/cativo23/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@server/api/analyze.post.ts
@database/migrations/20260216000001_fix_credit_deduction_race_condition.sql
@database/migrations/20260217000001_atomic_credit_increment.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create email verification trigger migration</name>
  <files>database/migrations/20260303000002_add_email_verification_trigger.sql</files>
  <action>Create a migration file that implements a PostgreSQL trigger function to award 10 free credits when a user verifies their email.

The trigger should:
1. Detect when email_confirmed_at changes from NULL to a value in the auth.users table (indicating email verification)
2. Update the corresponding user in the public.users table to add 10 credits using an atomic operation
3. Set free_credits_awarded to TRUE and free_credits_at to the current timestamp
4. Only run if free_credits_awarded is FALSE or NULL (to prevent multiple awards)
5. Implement proper atomic locking to prevent race conditions when multiple requests occur simultaneously

Create the trigger that runs AFTER UPDATE OF email_confirmed_at ON auth.users for each row.

Reference existing migration 20260217000001_atomic_credit_increment.sql for proper atomic operation patterns to prevent race conditions.</action>
  <verify>npx vitest run tests/integration/free-credits.spec.ts --reporter=verbose</verify>
  <done>Email verification trigger properly implemented with atomic operations to prevent race conditions and attached to auth.users table</done>
</task>

<task type="auto">
  <name>Task 2: Modify analysis endpoint to handle monthly free Basic analysis</name>
  <files>server/api/analyze.post.ts</files>
  <action>Update the analyze endpoint to support monthly free Basic analysis by:

1. Before calling process_analysis_transaction, check if:
   - analysis_type is 'basic'
   - User qualifies for a free analysis (check monthly_free_analysis_used and compare dates)

2. Add a helper function in the same file to determine if user can use a free analysis:
   - Check if current month/year matches monthly_free_analysis_reset_date
   - If not matched, reset the monthly_free_analysis_used flag to FALSE
   - If flag is FALSE, user can use free analysis
   - If flag is TRUE, user must pay normally

3. If user qualifies for free analysis:
   - Use an atomic database transaction to ensure monthly_free_analysis_used is set to TRUE AND the analysis is processed in one operation
   - This prevents race conditions where multiple simultaneous requests could both qualify as "free"
   - Call process_analysis_transaction with a credit cost of 0 (so no credits are deducted)

4. Preserve all existing validation and security measures (input validation, SSRF protection, etc.)

Make sure the free analysis only applies to 'basic' tier as per requirements. Pay special attention to race condition prevention by ensuring atomic updates to the monthly_free_analysis counter and flag.</action>
  <verify>npx vitest run tests/integration/monthly-free-analysis.spec.ts --reporter=verbose</verify>
  <done>Analyze endpoint handles monthly free Basic analysis with atomic operations to prevent race conditions while preserving all existing functionality</done>
</task>

</tasks>

<verification>
Test that email verification properly awards 10 credits and monthly free analysis works for Basic tier with race condition prevention. Verify with: npx vitest run tests/integration/monthly-free-analysis.spec.ts
</verification>

<success_criteria>
Users receive 10 credits on email verification and can use 1 free Basic analysis per month with proper race condition prevention
</success_criteria>

<output>
After completion, create `.planning/phases/05-free-credits-onboarding/05-02-SUMMARY.md`
</output>
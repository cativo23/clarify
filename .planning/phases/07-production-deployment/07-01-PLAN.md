---
phase: 07-production-deployment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .env.example
  - server/plugins/worker.ts
  - server/api/health.get.ts
autonomous: true
requirements:
  - DEPLOY-01

must_haves:
  truths:
    - "Production environment template exists for deployment"
    - "Worker concurrency is configurable via environment variable"
    - "Health endpoint verifies actual Redis connectivity"
  artifacts:
    - path: ".env.example"
      provides: "Environment variable template with all required secrets"
      min_lines: 20
    - path: "server/plugins/worker.ts"
      provides: "Worker plugin with environment-based concurrency"
      contains: "BULLMQ_CONCURRENCY"
    - path: "server/api/health.get.ts"
      provides: "Health endpoint with Redis ping verification"
      exports: ["default"]
  key_links:
    - from: "server/plugins/worker.ts"
      to: "process.env.BULLMQ_CONCURRENCY"
      via: "environment variable read"
      pattern: "BULLMQ_CONCURRENCY"
    - from: "server/api/health.get.ts"
      to: "getRedisConnection"
      via: "import and ping"
      pattern: "getRedisConnection|redis\\.ping"
---

<objective>
Complete production deployment readiness by creating environment template, enabling configurable worker concurrency, and implementing real health checks.

Purpose: DEPLOY-01 requires verified production deployment with all environments configured. Three gaps block deployment: missing .env.example template, hardcoded worker concurrency, and placeholder health check.

Output: .env.example template, worker.ts reading BULLMQ_CONCURRENCY, health.get.ts with Redis ping
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-production-deployment/07-CONTEXT.md
@.planning/phases/07-production-deployment/07-RESEARCH.md
@.planning/phases/07-production-deployment/07-VALIDATION.md

@docker-compose.prod.yml
@Dockerfile
@DEPLOY.md
@server/utils/queue.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create .env.example template</name>
  <files>.env.example</files>
  <read_first>
    - docker-compose.prod.yml (environment variable references)
    - DEPLOY.md (required variables section)
  </read_first>
  <action>
    Create .env.example with all environment variables required for production deployment.

    Include these exact variables with descriptive comments and placeholder values:

    ```
    # ============================================
    # Clarify Production Configuration
    # ============================================
    # Copy this file to .env and fill in your production values
    # NEVER commit .env to git - it's already in .gitignore

    # ============================================
    # Supabase (Authentication & Database)
    # ============================================
    SUPABASE_URL=https://your-project.supabase.co
    SUPABASE_ANON_KEY=your-anon-key-from-supabase-dashboard
    SUPABASE_SERVICE_KEY=your-service-role-key-from-supabase-dashboard

    # ============================================
    # OpenAI (AI Analysis)
    # ============================================
    OPENAI_API_KEY=sk-your-openai-api-key

    # ============================================
    # Stripe (Payment Processing)
    # ============================================
    STRIPE_SECRET_KEY=sk_live_your-stripe-secret-key
    STRIPE_PUBLISHABLE_KEY=pk_live_your-stripe-publishable-key
    STRIPE_WEBHOOK_SECRET=whsec_your-webhook-secret

    # ============================================
    # Redis (BullMQ Queue Backend)
    # Use Upstash for production (TLS required) or local Redis
    # ============================================
    REDIS_HOST=your-redis-host.upstash.io
    REDIS_PORT=6379
    REDIS_TOKEN=your-redis-auth-token

    # ============================================
    # Application
    # ============================================
    BASE_URL=https://clarify.yourdomain.com

    # ============================================
    # Worker Configuration
    # ============================================
    BULLMQ_CONCURRENCY=2
    ```

    All placeholders must use the `your-*` pattern to make it obvious they need to be replaced.
  </action>
  <verify>
    <automated>test -f .env.example && grep -q "SUPABASE_URL=" .env.example && grep -q "OPENAI_API_KEY=" .env.example && grep -q "STRIPE_SECRET_KEY=" .env.example && grep -q "REDIS_TOKEN=" .env.example && echo ".env.example has all required variables"</automated>
  </verify>
  <done>
    .env.example file exists with all 11+ environment variables documented
  </done>
  <acceptance_criteria>
    - File exists at .env.example
    - Contains SUPABASE_URL, SUPABASE_ANON_KEY, SUPABASE_SERVICE_KEY
    - Contains OPENAI_API_KEY
    - Contains STRIPE_SECRET_KEY, STRIPE_PUBLISHABLE_KEY, STRIPE_WEBHOOK_SECRET
    - Contains REDIS_HOST, REDIS_PORT, REDIS_TOKEN
    - Contains BASE_URL
    - Contains BULLMQ_CONCURRENCY
    - Has section headers with comments explaining each group
    - Uses your-* placeholder pattern for sensitive values
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make worker concurrency configurable</name>
  <files>server/plugins/worker.ts</files>
  <read_first>
    - server/plugins/worker.ts (current hardcoded concurrency: 2)
    - docker-compose.prod.yml (BULLMQ_CONCURRENCY=2 environment)
  </read_first>
  <behavior>
    - Test: Worker reads concurrency from process.env.BULLMQ_CONCURRENCY
    - Test: Falls back to 2 if env var not set
    - Test: Parses integer correctly (no NaN on invalid input)
  </behavior>
  <action>
    Modify the Worker initialization in server/plugins/worker.ts line 149 to read concurrency from environment variable.

    Current code (line 149):
    ```typescript
    concurrency: 2, // Process 2 jobs at the same time
    ```

    Replace with:
    ```typescript
    concurrency: parseInt(process.env.BULLMQ_CONCURRENCY || '2', 10),
    ```

    This allows runtime configuration via docker-compose.prod.yml while maintaining backward compatibility with the default value of 2.

    Verify the change is in the Worker constructor options object, not elsewhere in the file.
  </action>
  <verify>
    <automated>grep "parseInt(process.env.BULLMQ_CONCURRENCY" server/plugins/worker.ts && echo "Concurrency is now configurable"</automated>
  </verify>
  <done>
    Worker concurrency reads from BULLMQ_CONCURRENCY env var with fallback to 2
  </done>
  <acceptance_criteria>
    - server/plugins/worker.ts contains: parseInt(process.env.BULLMQ_CONCURRENCY || '2', 10)
    - No hardcoded concurrency: 2 in Worker constructor options
    - Fallback value '2' is present in the parseInt call
    - radix 10 is specified in parseInt call
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement real Redis health check</name>
  <files>server/api/health.get.ts</files>
  <read_first>
    - server/api/health.get.ts (current placeholder implementation)
    - server/utils/queue.ts (getRedisConnection export)
  </read_first>
  <behavior>
    - Test: Health endpoint pings Redis connection
    - Test: Returns status: "ok" when Redis connected
    - Test: Returns status: "degraded" when Redis disconnected
    - Test: Returns 503 HTTP status when unhealthy
    - Test: Response includes services.redis field with connection status
  </behavior>
  <action>
    Replace the placeholder health check with actual Redis connectivity verification.

    Current implementation returns hardcoded "connected" without verification.

    New implementation must:
    1. Import getRedisConnection from "../utils/queue"
    2. Try to ping Redis using redis.ping()
    3. Set health.services.redis = "connected" on success
    4. Set health.services.redis = "disconnected" on error and change status to "degraded"
    5. Throw 503 error with createError if Redis is disconnected
    6. Return health object with actual status

    Implementation:
    ```typescript
    import { getRedisConnection } from "../utils/queue";

    export default defineEventHandler(async () => {
      const health = {
        status: "ok" as const,
        services: {
          database: "unknown" as const,
          redis: "unknown" as const,
          ai: "active" as const,
        },
        timestamp: new Date().toISOString(),
      };

      try {
        const redis = getRedisConnection();
        await redis.ping();
        health.services.redis = "connected";
      } catch {
        health.services.redis = "disconnected";
        health.status = "degraded";
      }

      if (health.services.redis === "disconnected") {
        throw createError({
          statusCode: 503,
          statusMessage: "Service Unhealthy",
          body: health,
        });
      }

      return health;
    });
    ```

    This implements D-13 (health check endpoint) and addresses Pitfall 3 from RESEARCH.md.
  </action>
  <verify>
    <automated>grep "getRedisConnection" server/api/health.get.ts && grep "redis.ping()" server/api/health.get.ts && echo "Health check now verifies Redis"</automated>
  </verify>
  <done>
    Health endpoint performs actual Redis ping and returns accurate connectivity status
  </done>
  <acceptance_criteria>
    - server/api/health.get.ts imports getRedisConnection from "../utils/queue"
    - Contains await redis.ping() or equivalent ping call
    - Sets health.services.redis to "connected" or "disconnected" based on result
    - Throws createError with statusCode 503 when Redis disconnected
    - No hardcoded "connected" placeholder comments remain
  </acceptance_criteria>
</task>

</tasks>

<verification>
After implementation:
1. .env.example exists with all required variables
2. Worker reads BULLMQ_CONCURRENCY from environment
3. Health endpoint verifies Redis connectivity

Verify with:
```bash
# Check .env.example
test -f .env.example && wc -l .env.example

# Check worker concurrency
grep "BULLMQ_CONCURRENCY" server/plugins/worker.ts

# Check health endpoint
grep "getRedisConnection" server/api/health.get.ts
```
</verification>

<success_criteria>
- [ ] .env.example created with 11+ environment variables
- [ ] Worker concurrency configurable via BULLMQ_CONCURRENCY
- [ ] Health endpoint performs actual Redis ping
- [ ] All changes verified via grep commands
- [ ] DEPLOY-01 requirement addressed: deployment artifacts ready
</success_criteria>

<output>
After completion, create .planning/phases/07-production-deployment/07-01-SUMMARY.md documenting:
- .env.example created (line count, variables included)
- Worker concurrency now reads from environment
- Health check implementation details
- Ready for manual deployment per DEPLOY.md
</output>
